import { PlusSquare } from 'lucide-react';
import { Button } from '../ui/button';

export const Main = () => {
  return (
    <div>
      <Button>
      <PlusSquare />
      </Button>
    </div>
  );
};
